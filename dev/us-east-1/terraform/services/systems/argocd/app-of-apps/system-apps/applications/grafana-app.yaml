apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.grafana.name }}
  namespace: {{ .Values.global.namespace }}
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: {{ .Values.grafana.destination.namespace }}
    server: "https://kubernetes.default.svc"
  source:
    path: {{ .Values.grafana.path }}
    repoURL: {{ .Values.grafana.repoURL }}
    targetRevision: {{ .Values.grafana.targetRevision }}
    helm:
      valueFiles:
        - "values.yaml"
  project: "devops"
  syncPolicy:
    managedNamespaceMetadata:
      labels:
        managed: "argo-cd"
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        maxDuration: 3m0s
        factor: 2