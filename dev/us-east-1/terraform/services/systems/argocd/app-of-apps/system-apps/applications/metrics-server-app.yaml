apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.metrics-server.name }}}
  namespace: {{ .Values.global.namespace }}
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: {{ .Values.metrics-server.destination.namespace }}
    server: {{ .Values.metrics-server.destination.server }}
  source:
    path: {{ .Values.metrics-server.path }}
    repoURL: {{ .Values.metrics-server.repoURL }}
    targetRevision: {{ .Values.metrics-server.targetRevision }}
    helm:
      valueFiles:
        - "values.yaml"
  project: "devops"
  syncPolicy:
    managedNamespaceMetadata:
      labels:
        managed: "argo-cd"
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        maxDuration: 3m0s
        factor: 2